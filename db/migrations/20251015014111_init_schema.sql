-- migrate:up
-- Cole aqui TODO o seu script do Casamenteiro (com CREATE EXTENSION, ENUMs, TABELAS, VIEWS, FKs, TRIGGERS etc.)
-- Como seu script já usa muitos IF NOT EXISTS, é perfeito para rodar em bancos limpos ou já-iniciados.

-- migrate:down
-- Opcional (pode deixar vazio por enquanto, ou só dropar algumas coisas se quiser).
