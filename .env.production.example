APP_ENV=production
HOST=0.0.0.0
PORT=3000

# DB de produção (preencha via Secrets Manager / variables do provedor)
DATABASE_URL=postgres://prod_user:prod_pass@<host>:5432/casamenteiro_prod?sslmode=require

# Em produção, **NUNCA** versionar valores reais
JWT_SECRET=prod_set_in_secret_manager
